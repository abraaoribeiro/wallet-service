server:
    port: 8082

spring:
  application:
    name: 'account'
  datasource:
    password: root
    username: root
    url: 'jdbc:mysql://localhost:3306/account?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true'
  jpa:
    hibernate:
      ddl-auto: update

eureka:
    client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
        defaultZone: http://localhost:8761/eureka